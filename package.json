{
  "name": "qcrecord-db",
  "version": "1.0.0",
  "description": "File QC DB",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "baseline": "flyway -X -configFile=flyway.conf baseline",
    "migrate": "flyway -X -configFile=conf/flyway.conf migrate"
  },
  "repository": {
    "type": "git"
  },
  "author": "Heather Armstrong",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.2",
    "debug": "^3.1.0",
    "dotenv": "^4.0.0",
    "express": "^4.16.2",
    "flywaydb-cli": "^0.5.0",
    "morgan": "^1.9.0",
    "pg": "^7.3.0",
    "pg-promise": "^7.2.1"
  },
  "devDependencies": {
    "eslint": "^4.10.0"
  }
}
